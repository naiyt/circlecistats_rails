<h1><a href="https://github.com/naiyt/circlecistats_rails">CircleCI Stats</a>, version 0.0.1</h1>

<h2>TODO: Filter by dates and branches, graph change over time</h2>

<h2>Average build time: <%= @average %> minutes, over <%= @builds.length %> builds</h2>
<h2>Average master: <%= @master_average %> minutes</h2>
<h2>Average success: <%= @average_success %></h2>
<h2>Average failures: <%= @average_failure %></h2>

<h3>Longest succesful build time:</h3>

<%= render "shared/build", :build => @maximum %>

<h3>Shortest succesful build time:</h3>

<%= render "shared/build", :build => @minimum %>

<h3>Other stats:</h3>

<ul>
  <li>Number of succesful builds: <%= @successes %></li>
  <li>Number of failed builds: <%= @failures %></li>
  <li>Number of canceled builds: <%= @cancels %></li>
</ul>

<h3>Averages by branch:</h3>

<table>
	<tr><th>Branch</th><th>Average</th></tr>
	<% @stats_by_branch.each do |branch, average| %>
		<tr><td><%= branch %></td><td><%= average %></td></tr>
	<% end %>
</table>